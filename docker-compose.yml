services:
  self-rag:
    build:
      context: .
      dockerfile: Dockerfile
    image: self-rag-from-scratch:latest
    container_name: self-rag-from-scratch
    env_file:
      - .env
    environment:
      - USER_AGENT=self-rag-from-scratch/1.0
    command: ["python", "SELFRAG-agent.py"]
